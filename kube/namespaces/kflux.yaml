---

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: namespaces
  namespace: flux-system
spec:
  interval: 5m
  path: "./kube/namespaces"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn: []
